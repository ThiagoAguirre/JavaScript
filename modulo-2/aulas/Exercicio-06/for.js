const comida = ["arroz", "feijao", "maça", "carne"];

for (let i = 0; i < comida.length; i++) {
  // const par = i % 2 === 0 ? 'par' : 'impar';
  // console.log(i,par);
  console.log(`indice ${i}`, comida[i]);
}
