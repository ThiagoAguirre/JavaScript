function soma(x, y) {
  return x + y;
}

console.log(soma(1, 2));

const raiz = (n) => n ** 0.5;

console.log(raiz(9));

const raizDois = function (z) {
  return z ** 0.5;
};

console.log(raizDois(9));
